name: beacon_detector
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+3

environment:
  sdk: ^3.12.2

dependencies:
  dchs_flutter_beacon: ^0.6.10
  flutter:
    sdk: flutter
  flutter_blue_plus: ^2.3.8
  flutter_local_notifications: ^22.0.1
  permission_handler: ^12.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
